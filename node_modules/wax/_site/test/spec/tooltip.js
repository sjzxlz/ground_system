describe('tooltip', function() {
  beforeEach(function() {
    t = wax.tooltip();
  });

  it('should create an element on select', function() {
      expect(wax.util.isString('Tom')).toEqual(true);
      expect(wax.util.isString(2)).toEqual(false);
  });

});
